<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="PaletteUI.Views.RootContent.MainContent.SideBarNavigation"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PaletteUI.Views.RootContent.MainContent"
    xmlns:core="using:PaletteUI.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel>
        <ItemsRepeater ItemsSource="{x:Bind Directories}">
            <ItemsRepeater.ItemTemplate>
                <DataTemplate x:DataType="core:FileViewModel">
                    <Button Content="{x:Bind Name}" CommandParameter="{x:Bind Path}" Click="Button_Click"
                            HorizontalAlignment="Stretch" Width="200"/>
                </DataTemplate>
            </ItemsRepeater.ItemTemplate>
        </ItemsRepeater>

    </StackPanel>
</UserControl>
